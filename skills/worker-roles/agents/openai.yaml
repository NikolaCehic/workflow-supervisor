interface:
  display_name: "Worker Roles"
  short_description: "Define narrow agent role contracts"
  default_prompt: "Use $worker-roles to assign strict roles and prevent role bleed in this workflow."

policy:
  allow_implicit_invocation: false
