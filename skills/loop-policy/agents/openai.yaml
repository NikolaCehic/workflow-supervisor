interface:
  display_name: "Loop Policy"
  short_description: "Control retries, gates, and continuation"
  default_prompt: "Use $loop-policy to define execution, retry, approval, and stopping rules for this workflow."

policy:
  allow_implicit_invocation: false
