interface:
  display_name: "Dossier Builder"
  short_description: "Create concrete handoff contracts"
  default_prompt: "Use $dossier-builder to create a grounded handoff dossier for this work unit."

policy:
  allow_implicit_invocation: false
