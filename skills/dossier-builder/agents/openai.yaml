interface:
  display_name: "Dossier Builder"
  short_description: "Create concrete worker delegation contracts"
  default_prompt: "Use $dossier-builder to create a concrete validated worker dossier for this already-bounded work unit."

policy:
  allow_implicit_invocation: false
