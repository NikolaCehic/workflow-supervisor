interface:
  display_name: "Workflow Supervisor"
  short_description: "Verify delegated Codex and Claude work"
  default_prompt: "Use $workflow-supervisor to choose the lightest route and require evidence before accepting delegated work."

policy:
  allow_implicit_invocation: false
