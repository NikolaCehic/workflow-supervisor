interface:
  display_name: "Workflow Supervisor"
  short_description: "Run lean, strict, or planning-only supervised workflows"
  default_prompt: "Use $workflow-supervisor to route this work proportionally, infer safe reversible defaults, ask only material authority questions, and verify the outcome with evidence."

policy:
  allow_implicit_invocation: false
