interface:
  display_name: "Workflow Supervisor"
  short_description: "Coordinate grounded agent workflow loops"
  default_prompt: "Use $workflow-supervisor to coordinate this open-ended workflow from grounded sources to verified outcome."

policy:
  allow_implicit_invocation: false
