{
  "agent": "claude-code",
  "status": "portable",
  "defaultTarget": "${CLAUDE_CONFIG_DIR:-~/.claude}/skills",
  "metadata": "SKILL.md",
  "fallback": "workflow-supervisor emit-context --agent claude-code --out CLAUDE.md",
  "delegate": {
    "command": ["claude", "-p", "--disable-slash-commands", "--strict-mcp-config", "--output-format", "json", "--no-session-persistence"],
    "versionArgs": ["--version"],
    "promptMode": "stdin",
    "schemaMode": "json",
    "schemaFlag": "--json-schema",
    "roleArgs": {
      "implementer": ["--permission-mode", "acceptEdits"],
      "verifier": ["--permission-mode", "plan"],
      "repair": ["--permission-mode", "acceptEdits"],
      "documenter": ["--permission-mode", "acceptEdits"]
    }
  }
}
